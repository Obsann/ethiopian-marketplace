export const metadata = {
  title: 'Listing',
  description: 'View a second-hand listing on SuqET.',
};

export default function ListingDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
