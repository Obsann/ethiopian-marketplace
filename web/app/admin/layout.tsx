export const metadata = {
  title: 'Admin',
  description: 'Review reports and seller verifications.',
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
