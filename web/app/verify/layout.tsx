export const metadata = {
  title: 'Verify seller',
  description: 'Submit ID for seller verification.',
};

export default function VerifyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
