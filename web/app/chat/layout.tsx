export const metadata = {
  title: 'Messages',
  description: 'Chat with buyers and sellers on SuqET.',
};

export default function ChatLayout({ children }: { children: React.ReactNode }) {
  return children;
}
