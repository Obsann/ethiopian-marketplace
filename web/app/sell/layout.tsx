export const metadata = {
  title: 'Sell an item',
  description: 'Create a new listing on SuqET.',
};

export default function SellLayout({ children }: { children: React.ReactNode }) {
  return children;
}
