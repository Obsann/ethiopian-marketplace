export const metadata = {
  title: 'Account',
  description: 'Log in or create a SuqET account.',
};

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
